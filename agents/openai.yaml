interface:
  display_name: "SSH Remote Control"
  short_description: "SSH access, collaborators, and HTTPS operations"
  default_prompt: "Use $ssh-remote-control to resolve the server alias, diagnose SSH/SFTP access, add scoped collaborators, and deploy trusted HTTPS."
